    import * as constants from "../constants";
//  import * as constants from "../constants";

    export type VideoFormat = typeof constants.videoFormats[number];
//  export type VideoFormat = typeof constants.videoFormats[number];
    export type ImageFormat = typeof constants.imageFormats[number];
//  export type ImageFormat = typeof constants.imageFormats[number];
